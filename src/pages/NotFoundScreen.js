import React from "react";

const NotFoundScreen = () => {
  return <h3 style={Styles.container}>Halaman tidak ditemukan</h3>;
};

const Styles = {
  container: {
    textAlign: "center",
  },
};

export default NotFoundScreen;
